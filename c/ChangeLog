2013-01-10  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[spit] Cache DTS position/term pairs.

	* spit.el (spit-%dump-dts): For each DTS NAME,
	cache position/term pairs in ‘spit--cache’ as dts:NAME.

2013-01-10  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[spit int] Don't cache ‘nid<-name’; rename other to simply ‘node-name’.

	* spit.el: Do ‘s/name<-nid/node-name/g’.
	(spit-%dump-index): Don't bother caching ‘nid<-name’ info.

2013-01-09  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Specify BLOBS-INDEX and BLOBS blocks.

	* mkinix (block-mangler one out): New proc.
	(block-mangler one): Expose ‘out’.
	(blobs): New proc.
	(inline-images!): Delete proc.
	(do-it!): Use ‘blobs’, include BLOBS-INDEX-LEN in COUNTS; if there
	are any, output BLOBS-INDEX before NODES and BLOBS proper, after.
	* retrieve (blobs-index-size): New proc.
	(in): Read BLOBS-INDEX if available; return it.
	(out): Take arg ‘blobs’.
	(out --dump): Display ‘blobs’ if any.

2013-01-09  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Add abstraction: ixcc!

	* mkixin (ixcc!): New proc.
	(do-it! dedup! zcoord!): Use ‘ixcc!’.

2013-01-09  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Overhaul temporary-file manglement.

	* mkixin: Import SRFI 13 ‘string-join’.
	(block-mangler): New proc.
	(how-to-handle): Return also a block management proc.
	(floating-environments, document-term-sets, labels):
	Take ‘mkblk’; rework block management portions; update callers.
	(do-it! mkblk): New proc.
	(do-it! fsys): Delete proc.
	(do-it!): Rework block management.

2013-01-08  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[spit] Make first-line centering align w/ second line.

	* spit.el (spit-mode): Preserve ‘fill-column’
	value around call to ‘kill-all-local-variables’.
	(spit): Set ‘fill-column’.

2013-01-06  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[spit] Center first line.

	* spit.el (spit--cache): Center line when done.
	(spit): Center first line.

2013-01-06  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[spit] Prettify ‘spit-%dump-meta’ output.

	* spit.el (spit-%dump-meta): ...here.

2013-01-06  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Allow elts of META ... DESCRIPTION to be a renderable sequence.

	* mkixin: Import SRFI 13 ‘string-take’.
	(initial): New proc, moved to top-level from...
	(nice-strings! initial): ...here.
	(rescue grab-xid! nice x!ent separate-on-newline): New proc.
	(rescue grab-xid! nice x!ent): Use it instead of ‘string-trim-both’.

2013-01-05  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Remove string if ‘trim-ws’ is given a string w/ all whitespace.

	* mkixin (nice-strings! final): Return ‘ls’, not ‘loc’.
	(nice-strings! trim-ws) <end>: If the resulting
	string has no non-whitespace chars, remove it entirely.

2013-01-05  Thien-Thi Nguyen  <ttn@gnuvola.org>

	a1-nf3-mixp: Fix bug: Respect ‘xml:space’ attribute.

	That is, when it has value "preserve".

	* a1-nf3-mixp (crunch current-xml:space-preserve): New proc.
	(crunch crunch-1) <element>: Push state of attribute
	‘xml:space’ being "preserve" before recursing; pop afterwards.
	(crunch crunch-1) <character-data>: Inhibit ‘no-s’ override
	if current element's attribute ‘xml:space’ has value "preserve".

2013-01-04  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[spit] Upgrade ‘name<-nid’ as side-effect of sectioning tree dump.

	That is, cache the fully-propertized node names,
	beyond mere ‘variable-pitch’.

	* spit.el (spit-%dump-index): If ‘name<-nid’ is already set,
	don't touch it again; also, hash the name w/ ‘variable-pitch’ face.
	(spit-%dump-dts pretty): New ‘flet’.
	(spit-%dump-dts): Use it instead of consulting
	‘cache’ directly and propertizing the name.
	(spit-%dump-s-tree): Save the fully-propertized name in the
	corresponding entry in ‘name<-nid’; update first line when done.

2013-01-04  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[repl] Add commands: forward, backward

	* retrieve (out repl move!): Rewrite.
	(out repl forward): New proc, shunted.
	(out repl backward): Likewise.
	* spit.el (spit-%backward, spit-%forward): New commands.
	(spit-mode-map): Bind them to "[" and "]", respectively.

2013-01-03  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Release: 1.6

2013-01-03  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Add provisionary Emacs Lisp library for visual vetting.

	* spit.el: New file.

2013-01-02  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Allow XID TITLE to be a renderable sequence.

	* mkixin (rescue grab-xid! x!string): Delete proc.
	(rescue grab-xid! nice-title): New proc.
	(rescue grab-xid!): Use ‘x!’ directly; use ‘nice-title’.

2013-01-02  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Make "retrieve repl" read forms, not lines.

	* retrieve (out dump-dts): If ‘name’ is a symbol, use it directly.
	(out repl loop): Use ‘read’; derive ‘cmd’ and ‘args’ from it.

2013-01-02  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Fix bug: Consider syn{,code}index for default font.

	This was an omission from 2012-12-28, "Handle
	XML from makeinfo 4.13.92 (TexinfoML V5.00)".

	* mkixin (document-term-sets default-font): Rewrite.
	(document-term-sets pluck type!/cur): New proc.
	(document-term-sets pluck type!/old): Likewise.
	(document-term-sets pluck): Use them.
	(IGNORABLE): Remove ‘synindex’, ‘syncodeindex’.
	(tuck extract-dts-setup segregate!): New proc.
	(tuck extract-dts-setup): Also extract ‘synindex’ and
	‘syncodeindex’ elements; return (SYNS . DEFS).
	(tuck w/o-useless-attributes): Add attribute
	‘incode’ for particular element ‘indexterm’.
	(tuck): Update returned DTS-SETUP value for not-cur case.

2013-01-02  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Synch DTD w/ Texinfo 4.13.93.

	* zomg (V5.0): Rename from ‘V5.00’.

2013-01-02  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Handle destructuring in ‘attribute-try’.

	* mkixin (attribute-try): If the attribute is found,
	pass a string to PROC instead of a list of one string element.
	(attribute-ref): Use ‘identity’.
	(document-term-sets collect-indexterm! pluck):
	Update call; use ‘attribute-ref’ directly.
	(do-it! dedup!): Use ‘attribute-ref’ directly.

2013-01-02  Thien-Thi Nguyen  <ttn@gnuvola.org>

	retrieve: Bump default byte-count to 128.

	* retrieve (main): ...here.

2012-12-31  Thien-Thi Nguyen  <ttn@gnuvola.org>

	mkixin: Fix bug: Consider ‘definitionterm’ also for cur.

	Regression introduced 2012-12-28,
	"Handle XML from makeinfo 4.13.92 (TexinfoML V5.00)".

	* mkixin (document-term-sets collect-indexterm! pluck):
	For cur, if ‘incode’ test fails, fall back to ‘definitionterm’ test.

2012-12-30  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Release: 1.5

2012-12-30  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Specify / handle external reference targets aka labels.

	* mkixin: Import SRFI 13 ‘string-every’; SRFI 14
	‘char-set:letter+diggt’, ‘char-set:ascii’, ‘char-set-intersection’,
	‘char-set-adjoin!’.
	(labels): New proc.
	(do-it!): Use it; include LABELS-LEN in COUNTS;
	if there are labels, arrange to output that block.
	* retrieve: Import SRFI 1 ‘car+cdr’.
	(dts-list, flosets-list): Update.
	(in): Read labels block if available; return it.
	(out): Take arg ‘labels’.
	(out --dump): Display labels if any.
	(out show-labels): New proc.
	(out repl loop): Provide command ‘show-labels’.
	(out): Likewise.

2012-12-29  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Drop META ATTRS.

	* mkixin (rescue): Discard XML attributes; don't return them.

2012-12-29  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Drop COUNTS TOP.

	* mkixin (do-it!): Don't detect or output ‘top’.
	* retrieve (dts-list, flosets-list): Update ‘counts’ access.

2012-12-29  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Expand willful ignorance.

	* mkixin (IGNORABLE): Add ‘macro’.
	(tuck): Take ‘nf’, not ‘ls’.
	(tuck neglecting-some-kids): New proc.
	(tuck w/o-macro): Delete proc.
	(tuck w/o-useless-attributes): New proc.
	(tuck w/o-stupid-spaces-attribute): Delete proc.
	(tuck): Ignore elements in IGNORABLE for the whole tree;
	update to use ‘w/o-useless-attributes’.
	(do-it!): Update call to ‘tuck’.

2012-12-29  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] mkixin: Ignore element ‘alias’.

	* mkixin (IGNORABLE): Add ‘alias’.

2012-12-29  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Specify / handle internal references.

	* mkixin: Import SRFI 13 ‘substring/shared’.
	(position): New object property.
	(positioned): New proc.
	(floating-environments write!): Use ‘positioned’.
	(document-term-sets manager write!): Likewise.
	(identify try): New proc.
	(identify id<-name): Use ‘try’.
	(identify): Set ‘(position nodes)’.
	(do-it! dedup!): New proc.
	(do-it! grok!): Use it.

2012-12-29  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Add abstractions: float-type, float-name

	* mkixin (float-type, float-name): New procs.
	(floating-environments plucker): Use them.

2012-12-29  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Generalize ‘simplified’ to work w/ ATOM, not just STRING.

	* mkixin (simplified): ...here.

2012-12-28  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Don't specify / handle ‘copying’ attribute for ‘titlepage’.

	* mkixin (rescue grab/dedup-copying!): ...here.

2012-12-28  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Handle XML from makeinfo 4.13.92 (TexinfoML V5.00).

	* mkixin: Import SRFI 1 ‘remove!’, ‘every’.
	(find-printindex): Take ‘cur?’; return proc; update caller.
	(rescue): Take ‘cur?’; update caller.
	(rescue grab-variables!): Handle ‘set’ vs ‘setvalue’.
	(rescue grab-copying!): New proc.
	(rescue): If ‘cur?’ don't bother w/ dedup.
	(floating-environments): Take ‘cur?’; update caller.
	(floating-environments plucker): Handle name in child vs attr.
	(document-term-sets): Take ‘cur?’, ‘setup’; update caller.
	(document-term-sets default-font): New proc.
	(document-term-sets manager): Use ‘default-font’.
	(document-term-sets collect-indexterm! pluck): Handle ‘incode’
	attr vs ‘definitionterm’ parent, ‘mergedindex’ vs ‘index’ attr.
	(section-tree): Take ‘cur?’; handle
	‘sectiontitle’ vs ‘title’; update caller.
	(IGNORABLE): New data structure.
	(tuck): New proc.
	(inline-images): Take ‘cur?’; update caller.
	(inline-images inline!): Handle ‘imagefile’ child vs attributes.
	(do-it!): Use ‘tuck’; obtain also ‘cur?’ and ‘dts-setup’.

2012-12-28  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Specify / handle whitespace normalization.

	* mkixin: Import SRFI 2 ‘and-let*’; SRFI 13 ‘string-drop’,
	‘string-index’, ‘string-skip’, ‘string-skip-right’;
	SRFI 14 ‘char-set:whitespace’.
	(attribute-try): New proc.
	(attribute-ref): Use ‘attribute-try’.
	(nice-strings!): New proc.
	(how-to-handle): Use ‘nice-strings!’.

2012-12-27  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Specify / handle META XID INVITATIONS.

	This replaces 2012-12-26, "Specify / handle META XID DIRENTRIES",
	which failed to handle multiple or no given categories.

	* mkixin: Import SRFI 1 ‘partition!’.
	(rescue grab-xid! x!invitations): New proc.
	(rescue grab-xid!): Process ‘@dircategory’ and ‘@direntry’
	together; return the combined list of ‘(CATEGORY [ENTRY...])’.

2012-12-27  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Warn if ‘rescue’ leaves any unprocessed forms.

	* mkixin (rescue grab-titlepage!): New proc.
	(rescue grab-toc!): Rename from ‘extract-toc’; make destructive.
	(rescue): Use new procs; if any forms remain unprocessed,
	display a warning and write the forms to the current error port.

2012-12-26  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Default META COPYING to ‘-’ (hyphen).

	* mkixin (rescue grab/dedup-copying!):
	If there are no forms, return ‘#f’.
	(rescue): If ‘copying’ is false, use ‘-’.

2012-12-26  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Drop POSITION from ‘float’ entry.

	* mkixin (floating-environments plucker): ...here.

2012-12-26  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Use side-effects less.

	* mkixin (rescue grab-xid!): Take arg LANG; use it for rv.
	(rescue): Update call to ‘grab-xid!’; don't jam XID directly.

2012-12-26  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Specify / handle META XID DIRENTRIES.

	* mkixin: Import SRFI 13 ‘string-trim-both’.
	(rescue grab-xid!): Rewrite; update caller.

2012-12-26  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Use ‘-’ (hyphen) for unspecified XID element.

	* mkixin (rescue grab-xid!): ...here, instead of ‘none’.

2012-12-24  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Redistribute DTDs from Texinfo 4.13, 4.13.92.

	* zomg (V4.13): Update entry to point in-tree.
	(V5.00): New entry.

2012-12-24  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] a1-nf3-mixp: Make ‘expand-file-name’ handle relative names.

	* a1-nf3-mixp (expand-file-name): ...here, by
	prefixing w/ the directory where the program is found.
	(parser): Use ‘expand-file-name’ for mapping file.

2012-12-24  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Look up DTD using PUBLIC-ID, not SYSTEM-ID.

	* a1-nf3-mixp (parser external-entity-ref): Use ‘public-id’.
	* zomg: Update.

2012-12-22  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Release: 1.4

2012-12-22  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Specify and handle LANG in META XID.

	* mkixin (rescue grab-lang!): New proc.
	(rescue): Include LANG in XID; if specified, also add to SETTINGS.

2012-12-20  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Specify and handle "image inlining".

	* mkixin: Import SRFI 13 ‘string-suffix-ci?’,
	‘(ice-9 popen)’ procs, ‘(ice-9 rdelim) read-delimited’.
	(string<-shell-command, hang!, inline-images!): New procs.
	(do-it!): Inline images before writing nodes.

2012-12-19  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Specify and handle "float sets".

	* mkixin (find-listoffloats, floating-environments): New procs.
	(do-it!): Keep track of float sets, lonely ones, too.
	(do-it! grok!): Note any floats.
	(do-it!): If there are any, output them, as well.
	* retrieve (flosets-list): New proc.
	(in): Read flosets; return them.
	(out): Take flosets.
	(out --dump): Display flosets, if any.

2012-12-19  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Break out subroutines useful for both DTS and floats.

	* mkixin (attribute-ref): New proc.
	(symbol<-attribute-ref): Use ‘attribute-ref’.
	(at-first, symbol/hyphen<-string): New procs.
	(find-printindex): Use ‘at-first’.
	(simplified): New proc.
	(document-term-sets manager keyed/simplify!): Use ‘simplified’.
	(two-phase-accumulator): New proc.
	(do-it!): Use ‘two-phase-accumulator’.
	(do-it! grok! note-summary!): New proc.
	(do-it! grok!): Use ‘note-summary!’.

2012-12-18  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Use ‘kids’ more.

	* mkixin (find-printindex): Use ‘kids’.

2012-12-18  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Move S-TREE immediately after NODE-INDEX.

	* mkixin (do-it!): Output section tree length after ‘Top’
	node identifier; output section tree after node-index.
	* retrieve (dts-list): Update.
	(in): Read section tree after reading node index;
	in rv, move ‘s-tree’ after ‘index’.
	(out): Update param order; display ‘s-tree’ after ‘index’.

2012-12-18  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Say "node-index" for retrieve dump output.

	* retrieve (out --dump): ...here.

2012-12-18  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Specify automagic conversion for old-style txi* vars.

	* mkixin (txi-vars-as-settings): New proc.
	(rescue): Use it to adjust ‘variables’, ‘settings’.

2012-12-18  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Change META last element to TOC.

	* mkixin (rescue extract-toc): New proc.
	(rescue): Use it for last element of rv.

2012-12-17  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Specify SXML ‘insertcopying’ instead of string.

	* mkixin (rescue grab/dedup-copying!): Use ‘(insertcopying (@))’.

2012-12-17  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Call the DTS name ‘name’ instead of ‘type’.

	* mkixin (document-term-sets manager): ...here, in param name.
	* retrieve (in go): Likewise.
	(out): Rename param from ‘type’ to ‘details’.

2012-12-17  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Add DTS "default font" and "inverse default font".

	* mkixin (walker/cozy): New proc.
	(document-term-sets font): New object property.
	(document-term-sets manager): Compute default font.
	(document-term-sets manager keyed/simplify!): Set entry's ‘font’.
	(document-term-sets manager merge-duplicate-terms!):
	Expand equality predicate to include ‘font’.
	(document-term-sets manager write! pp): New proc.
	(document-term-sets manager write!): Output initial form; prefix ‘-’
	for entries with "inverse default font"; omit ‘count’ from rv list.
	(document-term-sets collect-indexterm! pluck): Update returned
	proc to take ‘parent’; if the parent is ‘definitionterm’, set the
	element's ‘font’ to ‘code’.
	(document-term-sets collect-indexterm!): Use ‘walker/cozy’;
	specify a procedure that takes two args and returns the first.
	(do-it! grok!): Don't specify DTS-COUNT for ‘lonely-dts’.
	* retrieve (in go): Don't take N.
	(in go entries): Take additional arg N.
	(in go): Include cdr of initial form as car of collected dts.
	(out dump-dts): Update accordingly.

2012-12-17  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Add DTS "default font" and "inverse default font".

	* mkixin (walker/cozy): New proc.
	(document-term-sets font): New object property.
	(document-term-sets manager): Compute default font.
	(document-term-sets manager keyed/simplify!): Set entry's ‘font’.
	(document-term-sets manager merge-duplicate-terms!):
	Expand equality predicate to include ‘font’.
	(document-term-sets manager write! pp): New proc.
	(document-term-sets manager write!): Output initial form; prefix ‘-’
	for entries with "inverse default font"; omit ‘count’ from rv list.
	(document-term-sets collect-indexterm! pluck): Update returned
	proc to take ‘parent’; if the parent is ‘definitionterm’, set the
	element's ‘font’ to ‘code’.
	(document-term-sets collect-indexterm!): Use ‘walker/cozy’;
	specify a procedure that takes two args and returns the first.
	(do-it! grok!): Don't specify DTS-COUNT for ‘lonely-dts’.
	* retrieve (in go): Don't take N.
	(in go entries): Take additional arg N.
	(in go): Include cdr of initial form as car of collected dts.

2012-12-16  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Replace term "tli" with "dts".

	* mkixin (document-term-sets): Rename from ‘two-letter-indices’;
	update temp file name.
	(do-it!): Update local var names.
	* retrieve (dts-list): Rename from ‘tli-list’.
	(in): Rename local var ‘tli’ to ‘dtsi’.
	(out): Likewise; rename commands ‘list-tli’ and
	‘dump-tli’ to ‘list-dts’ and ‘dump-dts’, respectively.

2012-12-12  Thien-Thi Nguyen  <ttn@gnuvola.org>

	retrieve: Bump default byte-count to 80.

	* retrieve (main): ...here.

2012-12-11  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Release: 1.3

2012-12-11  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Explore producing SXML w/o Guile.

	* SxprWriter.pm.patch: New file.
	* a1-nf1: New file.
	* a1-nf3-from-nf1: New file.

2012-12-10  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] mkixin: Do node{name,next,prev,up} extraction in one pass.

	* mkixin (xnode!): Delete proc.
	(identify four): New list of procs.
	(identify maybe-four!): New proc.
	(identify id<-name): New proc.
	(identify): Extract ‘nodename’, ‘nodenext’, ‘nodeprev’,
	‘nodeup’ in one pass; deref names to id numbers afterwards;
	return ‘locs’ as last value instead of ‘id<-name’.
	(do-it! grok!): Take arg ‘loc’ instead of doing
	‘nodenext’, ‘nodeprev’, ‘nodeup’ extraction.
	(do-it!): Update binding form for call to ‘identify’;
	update call to ‘grok!’.

2012-12-09  Thien-Thi Nguyen  <ttn@gnuvola.org>

	retrieve: Take EOF to mean "quit" for repl.

	* retrieve (out repl loop): On EOF, do ‘quit’.

2012-12-09  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Add abstraction: walker

	* mkixin (walker): New proc.
	(find-printindex, collect-indexterm!, section-tree): Use it.

2012-12-09  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Add abstractions: symbol<-attribute-ref, kids

	* mkixin (symbol<-attribute-ref, kids): New procs.
	(find-printindex): Use ‘kids’
	(rescue, two-letter-indices): Use ‘symbol<-attribute-ref’, ‘kids’.
	(section-tree): Use ‘kids’.

2012-12-08  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Rename a2ixin to mkixin; drop .xml handling.

	* mkixin: Rename from a2ixin;
	don't import ‘(ice-9 popen)’ procs;
	don't import SRFI 13 ‘string-suffix?’.
	(how-to-handle): Assume filename has form "STEM.sxml".

2012-12-08  Thien-Thi Nguyen  <ttn@gnuvola.org>

	Omit ‘node’ from node data.

	* a2ixin (do-it! grok! stash!): Output ‘(cdr node)’, not ‘node’.
	* retrieve (out show-node-tree): Cons ‘node’ onto ‘read’ form.

2012-12-08  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[int] Update ‘dump’ / ‘dump/abbrev’ output.

	Omission from 2012-12-05, "Save nodes as SXML".

	* retrieve (out --dump): Use "~A" for node data;
	if necessary, output fake double-quote and close-parens,
	preceded by " …"; don't output "[+ N]" in case of full output.

2012-12-08  Thien-Thi Nguyen  <ttn@gnuvola.org>

	[dist] Move programs to c/, documents to d/.

	* a1-nf3-guile2: Move here from parent dir.
	* a1-nf3-mixp: Likewise.
	* a2ixin: Likewise.
	* retrieve: Likewise.
	* z-fixed-pp.scm: Likewise.
	* zomg: Likewise.


Copyright (C) 2012 Thien-Thi Nguyen

Copying and distribution of this file, with or without modification,
are permitted provided the copyright notice and this notice are preserved.
